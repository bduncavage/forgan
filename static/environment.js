function Environment() {
}

Environment.prototype.start = function(seedMeasures, fitnessData) {
  // choose random segements
  var fitnessSegments = [];
  var randStart =
  for(var i = 0; i < seedMeasures.length; i++) {
    
}
